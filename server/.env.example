# Database — Neon.tech PostgreSQL (free tier)
# Sign up at https://neon.tech and create a project to get this URL
DATABASE_URL="postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/downwork?sslmode=require"

# JWT — CHANGE THESE IN PRODUCTION (use: openssl rand -hex 32)
JWT_ACCESS_SECRET="your-access-secret-here"
JWT_REFRESH_SECRET="your-refresh-secret-here"

# App
PORT=3000
NODE_ENV=production
CORS_ORIGIN=https://your-app.vercel.app

# Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=52428800
